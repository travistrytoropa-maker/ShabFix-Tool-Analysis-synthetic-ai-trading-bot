services:
  - type: web
    name: synthetic-ai-trading-bot
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT backend.server:app

    healthCheckPath: /health

    envVars:
      - key: PYTHON_VERSION
        value: 3.11